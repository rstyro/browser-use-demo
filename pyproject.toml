[project]
name = "browser-use-demo"
version = "0.1.0"
description = "Browser Use demo with Volc Engine API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "browser-use>=0.12.6",
    "langchain-openai>=0.1.0",
    "python-dotenv>=1.0.0",
]
